#import global_variables
#import bso_build
#import bso_run

case $1 in 
    --help )
        echo "Esta é a ajuda"
    ;;
    build )
        build ${@:2}
    ;;
    run )
        echo '' > ${_temp_script}
        run ${@:2}
    ;;
esac

if [ -f ${_temp_script} ] ; then
    rm ${_temp_script}
fi
