#import global_variables
#import bso_build
#import bso_run

case $1 in 
    --help )
        echo "Esta é a ajuda"
    ;;
    build )
        build ${@:2}
    ;;
    run )
        echo '' > ${_run_temp_file}
        run ${@:2}
    ;;
esac

#if [ -f ${_run_temp_file} ] ; then
    #rm ${_run_temp_file}
#fi
